import 'package:flutter/material.dart';

class LocationOverviewViewControllers extends StatelessWidget {
  const LocationOverviewViewControllers({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
